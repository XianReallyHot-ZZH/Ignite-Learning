# Phase N · 源码分析:<子系统名>(镜像 <Ignite 包路径>)

> 本文件是 **phase 源码分析模板**。复制为 `specs/phases/PNN-<短名>-analysis.md` 后填充。
> 定位:该层产物为**该 phase 的 Session 教学文档**提供 grounded 输入(见 roadmap §3 文档体系)。
> **纪律**:① 写之前派 Explore agent 深读对应 Ignite 源码;② 写之后核验所有引用路径/类名存在(防幻觉门);③ 保留 `file:line` 锚点(2.18.0,供对照,非强约束)。

**Phase / 子系统**:Phase N · <子系统>
**覆盖 Session**:S?? ~ S??
**镜像源码根**:`vendors/ignite/modules/core/src/main/java/org/apache/ignite/<包>`(下称 `<简称>/`)

---

## 1. 概览
<这个子系统在 Ignite 中的位置;一句话本质;为什么排在路线的这个位置(关联 roadmap 依赖锚点);覆盖哪些 Session;**本 phase 明确"不做什么"**(边界)。>

## 2. 核心类与包清单
> 分组表,每行:类 | 职责 | 锚点(`file:line`)。**只写与本 phase 相关的真实类**。
| 类 | 职责 | 锚点 |
|---|---|---|
| `<Class>` | <一句话职责> | `<file>.java`(:行) |
> 建议分组(按子系统裁剪):核心 / worker 或线程模型 / 关键数据结构 / 协议或编解码 / 可靠性或恢复 / 背压或流控 / 可选(SSL 等) / **消费者边界**(下游,通常只读不实现)。

## 3. 关键数据/控制流 trace
> 选该子系统最关键的 1~3 条流程,画成 step-by-step(wire→app / app→wire / 恢复 / 交换 /…),每步标注涉及的类。
- 3.1 <流程名>:...
- 3.2 <流程名>:...
> 每条后加一句"**要点**"(非显然的设计意图)。

## 4. 关键设计与算法(为什么)
> 本节灵魂。列该子系统的核心设计决策,每条讲清 **why**(必要时配小演算):为什么用 X 而非 Y;某不变量怎么保证;某算法怎么工作。

## 5. 依赖与边界
- **上游依赖**:<应尽量无/少;说明它依赖什么、不依赖什么;关联依赖锚点解释"为什么能在这个位置独立做"。>
- **下游/消费者**:<谁拥有/使用它;它对外暴露的契约(接口/泛型参数)。>

## 6. 拆成 Session 的依据
> 按"隔离度 × 复杂度"递增切;每个 Session 给:范围、镜像要点、可运行验收。
| Session | 范围(本 phase 内) | 镜像要点 | 可运行验收 |
|---|---|---|---|
| **S??** | <范围> | <镜像哪些类/机制> | <demo + 单测> |
> 末尾一句:为什么这么切(每步可运行、是上一步的自然深化;符合北辰式 + 保真阶梯)。

## 7. 源码阅读路线
> 由外到内、由简到难的阅读顺序(编号列表),每项指明读哪个类、看什么。

## 8. 自检
- [ ] **引用路径全部存在**:<核验条数> 条 OK。
- [ ] **依赖主张与锚点一致**:<一句话说明>。
- [ ] **覆盖本 phase 全部 Session**:§6 给出拆分依据。
- [ ] **每步可运行可测**:各 Session 验收明确。
- [ ] **CS 学生曲线**:无单个 Session 过载;难点已拆分。

> 写完后:据此**顺序产出**各 Session 教学文档(`specs/sessions/SNN-<短名>.md`,从 `sessions/_TEMPLATE.md`),并在 roadmap 对应 Session 块挂 `**教学文档**:` 链接。
